/// @description Create the Key parent.

// Inherit the parent event
event_inherited();

/* Declare instance variables */
key_id		= 0;		// This will correspond to the Lock ID and the Player's 'hasKey' array.
